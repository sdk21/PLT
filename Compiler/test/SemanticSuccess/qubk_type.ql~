def func_test(qubk z) : qubk ret_name { 
        qubk a; 
        a = |00>; 
        a = z;
        ret_name = a; 
}
def compute():qubk try {
        
        qubk a;
        
        a= |001>;
        try=func_test(a);
}
