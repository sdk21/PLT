def func_test(int z) : int ret_name { 
        int a; 
        a = 5; 
        

        #now checking while with comment
        while(a leq 10)
        a=a+1;
        
        while(a neq 1)
        {
            # Comment, inside
            a = (a+1) % 42;
        }

        ret_name = a;


}
def compute():int trial{

trial = func_test(5);
}

